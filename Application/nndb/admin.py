from django.contrib import admin

from django.apps import AppConfig

class nndbConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'nndb'

# Register your models here.
